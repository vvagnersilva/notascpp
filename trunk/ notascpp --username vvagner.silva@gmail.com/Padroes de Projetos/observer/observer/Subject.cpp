#include "Observer.h"
#include "Subject.h"

namespace subject
{
   void Subject::registerObserver(observer::Observer* o)
   {
   }
   //
   void Subject::removeObserver(observer::Observer* o)
   {
   }
   //
   void Subject::notifyObservers()
   {
   }
}
